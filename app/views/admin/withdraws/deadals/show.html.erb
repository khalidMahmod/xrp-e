<div class="row">
  <div class="col-xs-12">
    <div class="panel panel-primary">
      <div class="panel-heading">
        <span><%= t('.withdraw', sn: @deadal.id) %></span>
      </div>
      <div class="panel-body">
        <%= description_for :withdraw do %>
          <%= item_for @deadal, :id %>
          <%= item_for @deadal, :created_at %>
          <%= item_for @deadal, :aasm_state_text %>
          <hr class="split">
          <%= item_for @deadal.member, :name %>
          <%= item_for @deadal, :fund_extra %>
          <%= item_for @deadal, :fund_uid do %>
            <span><%= @deadal.fund_uid.truncate(22) %></span>
          <% end %>
          <%= item_for @deadal, :amount %>
          <hr class="split">
          <%= item_for @deadal, :remark %>
          <hr class="split">
          <ul class="list-inline pull-right">
            <% if @deadal.may_reject? %>
              <li>
                <%= link_to t('.reject'), url_for([:admin, @deadal]), class: 'btn btn-danger', method: 'DELETE', confirm: t('.reject_confirm') %>
              </li>
            <% end %>
            <% if @deadal.may_process? %>
              <li>
                <%= link_to t('.process'), url_for([:admin, @deadal]), class: 'btn btn-primary', method: 'PATCH' %>
              </li>
            <% end %>
          </ul>
        <% end %>
      </div>
    </div>
  </div>

  <div class="col-xs-12">
    <%= render partial: 'shared/admin/member', locals: {member: @deadal.member} %>
    <%= render partial: 'shared/admin/account', locals: {account: @deadal.account} %>
  </div>
</div>